import { AnyAction, Reducer } from 'redux';

const global: Reducer = (state = {}, action: AnyAction) => {
    switch (action.type) {
        default:
        return state
    }
}
  
  export default global